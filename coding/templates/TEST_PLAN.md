# Test plan — REPLACE_TASK_ID

Status: template; not executed. Replace every placeholder before task execution. Commands below are fields to fill after implementation/review, not working product commands.

## Identity and scope

- Task / parent WP:
- Architecture baseline, section and requirement/test IDs:
- Source delivery route and exact candidate readback reference (human-Git / authorized direct GitHub):
- Base/candidate SHA (candidate may be pending during planning):
- Actual new source-CI workflow/check names and required execution subject; unavailable checks recorded explicitly:
- Artifact/build/dependency identities, when applicable:
- Clean source/build state and declared generated or other non-Git inputs:
- Receipt/transfer verification method; local exploratory edits excluded from qualification:
- Fixture/test implementation version:
- Supported profile and enabled capabilities:
- Explicitly excluded/deferred cases and reasons:
- Plan author / reviewing human / date (UTC):

## Prerequisites and safety

- Environment role, provider server ID reference and generation:
- Required OS/image/kernel/tool versions:
- Initial source/service/data state:
- Independent observer/controller and its availability:
- Required authority; permitted effects/resources; human server operator (GitHub authority is separate):
- Session isolation verified: no SSH agent/X11 forwarding, sensitive mounts or credential sockets:
- Permitted external connections/egress and narrowly scoped transfer credentials:
- Synthetic data/secrets and their custody:
- Alternate access/recovery procedure verified:
- Disruption, deadlines, abort criteria and cleanup:
- Missing inputs that block execution:

## Cases

| Case ID / architecture ID | Assertion and risk | Where / authority | Exact implemented command or manual steps | Expected exit and independent observations | Failure injection / positive control | Evidence / cleanup |
|---|---|---|---|---|---|---|
| REPLACE_CASE | REPLACE_ASSERTION | REPLACE_LOCATION | NOT YET IMPLEMENTED — fill and review | REPLACE_EXPECTATION | REPLACE_CASES | REPLACE_EVIDENCE |

For each case explain how it detects the relevant defect; do not simply restate an internal function's return. Identify timing, repeated observations and external vantage where needed. Separate command delivery from underlying operation outcome.

## Completion and invalidation

- Required pass criteria:
- Required cases still unimplemented/unavailable:
- Evidence classification, redaction, destination and retention:
- Changes/state transitions that invalidate receipts:
- Supported reasons for NOT_APPLICABLE:
- Reviewer-approved scope for retesting:
- Links to resulting receipts (empty until executed):
