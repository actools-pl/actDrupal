[build-system]
requires = ["setuptools==84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "actools-drupal"
dynamic = ["version"]
description = "Development package for the actDrupal fresh-install installer rewrite"
requires-python = ">=3.14,<3.15"
dependencies = [
  "PyYAML==6.0.3",
  "jsonschema==4.26.0",
  "rfc8785==0.1.4",
]
license = "MIT"
license-files = ["LICENSE", "NOTICE.md"]
authors = [{name = "Actools"}]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.14",
]

[project.scripts]
actools = "actools.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = false

[tool.setuptools.dynamic]
version = {attr = "actools.__version__"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["actools", "actools.*"]

[tool.setuptools.package-data]
"actools.contracts" = ["policies/*.json", "schemas/*.json"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]
pythonpath = ["src", "."]
