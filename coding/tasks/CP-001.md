# CP-001 — Package skeleton and source CI

**State:** planned. **Owner:** WP01 / WP25. **Milestone:** P01. **Depends on:** BOOT-001.

**Source/gate anchors:** v1.5.1 §§7.3, 13.1–13.6, 18.5 slice 1; G01/G09/G22; UX-R05/R06/R14; UX-0. These name requirements and proposed tests, not executed evidence.

Read [coder documentation](../00_START_HERE_CODER_DOCUMENTATION.md), [change control](../03_GITHUB_AND_CHANGE_CONTROL.md) and [test/evidence strategy](../05_TEST_AND_EVIDENCE_STRATEGY.md). Use this card in a fresh bounded coding chat; give the reviewer the actual candidate and relevant surrounding code.

## 1. Activation record

| Field | Initial value / rule |
|---|---|
| Exact repository and base commit | UNSET — obtain from Git after dependency acceptance |
| Task branch / operating route / bounded GitHub authority / human task owner / reviewer | UNSET |
| Current source automation classification and new-CI plan | UNSET — verify RB11 readiness and exact workflow paths |
| Accepted dependency evidence | UNSET — specific reviewed merge/contract outcome |
| Exact affected-file allowlist | UNSET — derive from current tree and scope below |
| Executable input contract/tool versions | UNSET — pin what this slice consumes |
| Test environment, commands and fixture identities | UNSET — no live target defaults |
| Exact operator/developer documentation paths | UNSET |
| Candidate / merged commit | UNSET |
| Review / evidence / documentation references | UNSET |

**Readiness:** coding begins only after these input fields are populated and dependencies verified. Candidate/merge/review/evidence outcome fields remain UNSET until produced. Resolve authorized routine implementation choices and record them; do not ask for permission again merely because this is a new chat. Never invent repository state, credentials, endpoints or passing tests. If an actual scope change is necessary, record the concrete reason and obtain the decision before implementing that change.

## 2. Outcome

A clean repository checkout builds and installs a minimal development package and runs the same documented source checks in CI. The only exposed CLI behavior is implemented, truthful version/help behavior.

## Required input package

- Merged BOOT package and accepted architecture/source hash.
- Observed new-repository layout and instructions; BOOT-000 root receipt and RB11 startup/automation records. BOOT must be integrated with required checks passed.
- Exact proposed source-CI triggers, permissions and runner ownership; current controls and any actual existing check contexts. Verify current settings rather than importing values from an earlier repository.
- Chosen supported development Python/build/test tooling with exact reviewed versions and dependency/license records.

## Allowed source areas — convert to an exact allowlist at activation

- A narrowly scoped new-source CI workflow under `.github/workflows/`; exact paths and effects belong in the activated allowlist. Root license/notices and adopted instruction templates are explicitly included in that list. Broader repository settings remain separately bounded.

## Implementation requirements

- Source checks address only the new implementation and its owned fixtures. Exclude historical reference documents and executable experiments from runtime/build artifacts. Do not add legacy check names, required imports or old source lookups.

## Meaningful proposed acceptance tests

- Build then install from a clean checkout/environment; run version/help without importing legacy files.
- Check wheel/package inventory excludes Git metadata, credentials and executable experimental seeds.
- Run the same named checks locally and in source CI; deliberate packaging/test fixture failure makes the relevant check fail. Use a separate controlled scanner-policy negative fixture, and verify required scan errors/unavailable execution cannot yield a passing vulnerability check. Do not execute exploit code to demonstrate a finding.
- Inspect actual workflow trigger/permission/runner behavior, action pins and dependency input closure; demonstrate routine source checks cannot provision Hetzner or obtain release/production authority. Record unobservable properties as gaps, not passing assertions.
- Non-TTY version/help output is plain, finite and secret-free; unsupported invocation reports an honest error.

## Documentation and changelog work

- Contributor setup and exact check commands, expected outcomes and supported tooling.
- Source-CI scope and real check names versus later installed release-test qualification. Distinguish a configured required check from a manual acceptance gate.
- Initial changelog entry describing a development skeleton, not a working installer.

## Excluded from this slice

- No deployment, firewall/SSH change, privileged executor, persistent journal or Drupal installation.
- No unscoped repository settings change, inherited source import, existing-site migration or unrelated repository operation.
- No published release/signing claims and no tests labeled full G01 PASS merely from a package smoke test.

## Coding, review and evidence handoff

Return a patch or complete changed-file package against the recorded base, or the fully verified direct-GitHub candidate under [RB12](../runbooks/RB12_DIRECT_GITHUB_OPERATIONS.md). In both routes supply the complete delivery manifest, actual base/candidate and full diff/tree, concise design explanation, exact test commands, expected assertions and documentation changes. State separately which checks were actually run, where, on which candidate and their results. If chat cannot execute them, label them pending and let the human run them. Do not say “all tests pass” from inspection alone.

The reviewer examines exact code plus surrounding interfaces and the test quality, then records findings with severity, consequence, correction and retest need. The human performs applicable devbox tests after review of privileged changes; these initial cards chiefly use isolated local/source-CI fixtures. A release-test installation is added when the installed slice exists, not fabricated for an earlier parser task.

For any shared schema, result contract or security-helper change, identify directly affected existing consumers and include meaningful consumer compatibility/integration checks alongside producer tests. If a consumer does not exist yet, record the gap and assign integration to its first consuming task. After a fix, identify invalidated evidence and rerun the affected checks. Update help/runbook/config/error examples when behavior changes. On acceptance record actual base/candidate/merge, reviewer disposition, evidence and docs references in `records/TASK_LEDGER.csv`. Retain any real merge fact while integration is pending/failing; status stays blocked until required post-merge checks pass. Update the user-facing changelog only with implemented behavior and known limits. No product-wide G gate becomes PASS merely because this slice's unit fixtures pass.
