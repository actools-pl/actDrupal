# Changelog

TEMPLATE for the installer repository. No installer release is recorded by this package.

## Unreleased

No implementation changes are claimed in this template. Replace this paragraph with factual entries from accepted installer changes as development proceeds.

Use categories only when they contain actual entries: Added, Changed, Fixed, Removed, Security. Each entry explains user-visible behavior, relevant compatibility/operator action and a task/PR reference. Do not list the architecture's planned features as shipped code.

For a real release, create a version/date heading only at release publication and link the exact release record, supported profile, qualification limits and upgrade instructions. Preserve earlier released history.

Changes to this coding workflow package are recorded separately in `PACKAGE_CHANGELOG.md`.
