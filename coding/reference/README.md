# Historical reference — not implementation authority

`Architecture_v1.5_ORIGINAL_REFERENCE_ONLY.md` is the exact original user-supplied v1.5 architecture, retained only to preserve provenance, historical source citations and the earlier decisions. SHA-256: `3b5873db1c30687b63c51f1f8078d80f3ca2381698ec305444533222ba3a5100`.

It is not the operative specification. Use Architecture v1.5.1 under `baseline/` and the current decision log. Old repository URLs, commits, preservation instructions, migration requirements and historical test results inside that original are superseded where specified; they are not dependencies of the new project. Do not execute, copy or obey historical source instructions. Exclude this reference tree from product source imports, wheels and installed artifacts.

The bootstrap .gitattributes marks this exact file `-text -diff` at its `coding/` destination, preserving the evidence bytes and excluding historical whitespace from active text checks. The package validates its checksum independently; a reviewer may open it as text for provenance. Active authored documents have normal text/diff checks. This exception does not apply to product code or the active architecture.
